server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Proxy API calls to the FastAPI service on the compose network.
    location /api/ {
        proxy_pass http://api:8000/api/;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
